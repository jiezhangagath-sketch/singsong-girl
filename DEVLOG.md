# 《海上花》互动体验 — 开发决策记录

> 本文档汇总项目从原型到当前版本的关键开发决策、修改要求与技术约定，供后续迭代参考。

---

## 一、项目定位

**产品形态**：基于《海上花列传》原著的单页网页互动体验游戏，以角色视角切入，通过选择推进剧情。

**技术栈**：纯前端（HTML + CSS + JS），数据驱动（`data/story.json`），部署于 GitHub Pages。

**核心设计原则**：
- **原著忠实**：场景内容必须基于原文摘录，事件顺序与原著一致。
- **去心理化**：`content` 为概括性叙述，禁止无原文依据的心理描写；解读只能放在 `source.commentary` 中。
- **纸质书美学**：UI 采用米黄纸张纹理、竖排感标题、 sepia 老照片滤镜，营造清末民国读物氛围。

---

## 二、开发阶段记录

### Phase 1：原型与四角色主线（51 场景）

**状态**：基础框架搭建，4 条角色线（赵朴斋、王莲生、黄翠凤、沈小红）。

**关键决策**：
- 采用 `fetch` 加载 `data/story.json`，因此必须通过本地 HTTP 服务器运行（`python3 -m http.server 8080`），禁止直接 `file://` 打开。
- 每个场景含 `content`（叙事）+ `source`（原文摘录+解读）+ `choices`（选项）。
- `choices` 中通过 `character` 字段实现角色选择，序幕以此渲染角色网格。

---

### Phase 2：扩展至全书（+32 场景，覆盖第 21–60 回）

**状态**：场景总数从 51 扩展到 83。

**关键决策**：
- 所有新增场景必须附带完整 `source` 原文引用，不允许杜撰原文。
- 修正早期场景的"幻觉"问题：移除无依据的动作描写、表情描写、内心独白。
- 王莲生线后期关键场景重写（如 ch51 打张蕙贞），融入核心解读洞察。

**叙事纪律确立**：
- `content` 只能是客观叙述或基于原文的概括。
- 涉及张爱玲、胡适等人引用需严格核对文献，禁止幻觉引用。
- 已修正 ch30/ch37/ch44 中的不准确引用。

---

### Phase 3：序幕视觉重构（角色选择界面）

**状态**：从纯文字按钮升级为 3×2 头像网格 + 浮动动画。

**关键决策**：
- 头像采用圆形裁剪（`border-radius: 50%; overflow: hidden;`），尺寸 100×100px（桌面）/ 72×72px（移动端）。
- 头像图片统一加 `sepia(18%) contrast(0.92)` 滤镜，hover 时移除，增强老照片质感。
- 头像加载优先级：`images/{角色名}.png` → `.jpg` → `.jpeg` → `.webp` → `portrait-{id}` 等 fallback。
- 加载失败时回退到 CSS 绘制的姓名首字（`.avatar-fallback`）。
- 添加序幕入场动画：墨晕开 + 聚光灯聚焦角色。

---

### Phase 4：隐藏角色系统（匡二 + 阿金）

**状态**：新增 2 个隐藏角色，总场景数达 **104**，总角色数 **6**。

#### 4.1 角色设定

| 角色 | 身份 | 关键情节 | 场景数 |
|---|---|---|---|
| **匡二** | 李鹤汀管家 | 评点屠明珠、跟踪四老爷到大兴里、与潘三私会、卷逃财物 | 6 |
| **阿金** | 周双珠家娘姨 | 与阿德保夫妻打架、巡捕捉赌时与张寿亭子间私会、叫子传情 | 4 |

#### 4.2 交互设计（多次迭代后的最终形态）

**初始设计**：
- 隐藏角色显示 CSS 绘制剪影（`brightness(0)` 黑色轮廓），首次点击揭示名字+描述并移除滤镜。

**用户反馈与调整**：
1. **剪影效果不佳** → 改为用户提供的清代绣像原图。
2. **全身像被圆形框截断** → 用 PIL 裁剪为 200×200px 头像（聚焦头部+上半身）。
3. **图片直接可见，但保持神秘感** → 去掉 `brightness(0)` 和 `sepia` 滤镜，直接显示原图。
4. **首次点击交互** → 名字初始显示为"隐藏人物"，首次点击后显示真实名字和身份描述，第二次点击进入剧情。

**最终交互逻辑**：
```
初始状态：显示原图头像，name="隐藏人物"，desc="点击揭示身份"
第一次点击：添加 revealed 类，name=真实名字，desc=身份描述，边框高亮
第二次点击：进入角色剧情线
```

#### 4.3 技术细节
- 隐藏角色在 `characters` 中标记 `"hidden": true` 与 `"gender": "male"/"female"`。
- CSS 中 `.hidden-character` 使用虚线边框，`.hidden-character.revealed` 切换为实线 accent 色边框 + `revealPulse` 动画。
- 隐藏角色头像不加 `sepia` 滤镜（`filter: none`），其他角色保留老照片效果。

---

### Phase 5：剧情增补与优化

#### 5.1 赵朴斋–阿巧线索链
- **新增 5 场景**：ch2_scene4、ch32_scene1、ch44_scene1、ch54_scene1、ch58_scene1。
- **作用**：从尚仁里初遇、阿巧跟局、到后来成婚，形成完整的伏笔回收链。

#### 5.2 黄翠凤剧情线
- **新增 3 场景**：cf_ch33_scene1（赎身定议）、cf_ch45_scene1（搬家拜祭）、cf_ch57_scene1（诡计得逞）。
- **修改 3 场景**：cf_ch55、cf_ch56、ending，使结局逻辑更贯通。

#### 5.3 匡二线扩展
- **新增 3 场景**：kuang_ch2_scene2（潘三私会）、kuang_ch3_scene2（还债嫁人）、kuang_ch4_scene1（卷逃）。
- **终章修改**：改为"衣锦荣归"，直接呼应跋文"潘三、匡二之衣锦荣归"。

---

### Phase 6：防缓存与部署优化

**问题**：浏览器缓存导致 story.json 和脚本更新不生效。

**解决方案**：
- `data/story.json` fetch 时附加 `?t=${Date.now()}` 时间戳。
- `index.html` 中 `<script src>` 使用版本号控制（`?v=N`），每次 JS/CSS 重大更新时递增。
- 提醒用户：如遇缓存问题，使用 `Cmd + Shift + R` 强制刷新或 DevTools 禁用缓存。

---

## 三、当前项目状态（截至最新推送）

| 指标 | 数值 |
|---|---|
| 总场景数 | 104 |
| 角色数 | 6（4 主 + 2 隐藏） |
| 主要角色 | 赵朴斋、王莲生、黄翠凤、沈小红 |
| 隐藏角色 | 匡二、阿金 |
| 线上地址 | https://jiezhangagath-sketch.github.io/singsong-girl/ |
| 本地运行 | `python3 -m http.server 8080` |

**最新推送 commit**：`5bb0aa2` — 阿金/匡二头像、隐藏角色交互优化、序幕张爱玲引言

---

## 四、关键文件说明

| 文件 | 职责 |
|---|---|
| `index.html` | 页面结构、版本号控制（`script.js?v=N`） |
| `style.css` | 纸质书风格 UI、角色选择网格、隐藏角色样式、响应式适配 |
| `script.js` | 游戏逻辑、序幕动画、角色网格渲染、隐藏角色揭示机制、进度保存 |
| `data/story.json` | 核心故事数据：角色定义 + 104 场景（content / source / choices / keyPeople） |
| `images/` | 角色头像（中文文件名，200×200px PNG） |

---

## 五、待确认/未来方向（非当前版本）

- [ ] 移动端体验进一步优化（当前头像 72px，在小屏上是否足够醒目）
- [ ] 阿金/匡二的清代绣像原图是否有版权风险（目前为古籍插图，属公有领域）
- [ ] 是否增加更多隐藏角色（如张寿、潘三等）
- [ ] 终章后的"跋文"展示是否需要独立页面

---

*记录整理时间：2026-05-22*
